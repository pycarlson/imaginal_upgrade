<div class="container">

  <div><%=@cookie %></div>

  <div class="feature-home-container">
    <% if flash[:alert] %>
      <p class="notice"><%= notice %></p>
    <% end %>

    <h2><%= @feature.title %></h2>

    <div class="vid-link">
        <%= render "shared/player", video: @feature, autoplay: false %>
    </div>
  </div>

  <div class="carousel-footer">

      <% @categories.each_with_index do |category, index| %>
        <%= render 'shared/carousel', title: category.name, subtitle: "TOP 20", id: index, videos: @carousel_videos[category.id] %>
      <% end %>

  </div>

</div>